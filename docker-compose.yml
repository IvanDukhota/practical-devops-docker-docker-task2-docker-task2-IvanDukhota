services:
  nodejs-server:
    build:
      context: ./api
      dockerfile: Dockerfile-dev
    ports:
      - "3080:3080"
    networks:
      default:
        aliases:
          - node-api

  angular-ui:
    build:
      context: ./ui
      dockerfile: Dockerfile-dev
    ports:
      - "4201:4201"
    depends_on:
      - nodejs-server
